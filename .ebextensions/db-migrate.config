container_commands:
  01_migrate:
    command: |
      env # just to print out the env variables for checking in logs
      source $PYTHONPATH/activate
      django-admin.py migrate
    leader_only: true
option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: Book.settings.local